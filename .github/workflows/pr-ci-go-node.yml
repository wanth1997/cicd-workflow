name: Reusable PR CI - Go Node

on:
  workflow_call:
    inputs:
      service_name:
        required: true
        type: string
      go_version_file:
        required: true
        type: string
      go_dependency_file:
        required: true
        type: string
      node_version:
        required: true
        type: string
      frontend_dir:
        required: true
        type: string
      frontend_lockfile:
        required: true
        type: string
      backend_fast_suite:
        required: true
        type: string
      backend_migration_suite:
        required: true
        type: string
      backend_ci_command:
        required: true
        type: string
      migration_check_command:
        required: true
        type: string
      frontend_ci_command:
        required: true
        type: string
      postgres_image:
        required: false
        type: string
        default: postgres:15
      postgres_db:
        required: false
        type: string
        default: app_ci
      postgres_user:
        required: false
        type: string
        default: app
      postgres_password:
        required: false
        type: string
        default: app
      artifact_retention_days:
        required: false
        type: number
        default: 14

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  backend-fast-tests:
    name: backend-fast-tests
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: ${{ inputs.go_version_file }}
          cache: true
          cache-dependency-path: ${{ inputs.go_dependency_file }}

      - name: Download Go modules
        run: go mod download

      - name: Run backend fast tests
        env:
          BACKEND_TEST_SUITE: ${{ inputs.backend_fast_suite }}
          GO_TEST_JSON: .artifacts/ci/backend-fast-go-test.json
        run: ${{ inputs.backend_ci_command }}

      - name: Upload backend fast Go test report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: ${{ inputs.service_name }}-backend-fast-go-test-report
          path: .artifacts/ci/backend-fast-go-test.json
          if-no-files-found: ignore
          retention-days: ${{ inputs.artifact_retention_days }}

  backend-migration-tests:
    name: backend-migration-tests
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: ${{ inputs.go_version_file }}
          cache: true
          cache-dependency-path: ${{ inputs.go_dependency_file }}

      - name: Download Go modules
        run: go mod download

      - name: Run backend migration regression tests
        env:
          BACKEND_TEST_SUITE: ${{ inputs.backend_migration_suite }}
          GO_TEST_JSON: .artifacts/ci/backend-migration-go-test.json
        run: ${{ inputs.backend_ci_command }}

      - name: Upload backend migration Go test report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: ${{ inputs.service_name }}-backend-migration-go-test-report
          path: .artifacts/ci/backend-migration-go-test.json
          if-no-files-found: ignore
          retention-days: ${{ inputs.artifact_retention_days }}

  backend-migration:
    name: backend-migration
    runs-on: ubuntu-latest
    timeout-minutes: 10
    services:
      postgres:
        image: ${{ inputs.postgres_image }}
        env:
          POSTGRES_DB: ${{ inputs.postgres_db }}
          POSTGRES_USER: ${{ inputs.postgres_user }}
          POSTGRES_PASSWORD: ${{ inputs.postgres_password }}
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U ${{ inputs.postgres_user }} -d ${{ inputs.postgres_db }}"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgres://${{ inputs.postgres_user }}:${{ inputs.postgres_password }}@localhost:5432/${{ inputs.postgres_db }}?sslmode=disable
      TEST_DATABASE_URL: postgres://${{ inputs.postgres_user }}:${{ inputs.postgres_password }}@localhost:5432/${{ inputs.postgres_db }}?sslmode=disable
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: ${{ inputs.go_version_file }}
          cache: true
          cache-dependency-path: ${{ inputs.go_dependency_file }}

      - name: Download Go modules
        run: go mod download

      - name: Run migration and schema check
        run: ${{ inputs.migration_check_command }}

  frontend-checks:
    name: frontend-checks
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ inputs.node_version }}
          cache: npm
          cache-dependency-path: ${{ inputs.frontend_lockfile }}

      - name: Run frontend checks
        run: ${{ inputs.frontend_ci_command }}
